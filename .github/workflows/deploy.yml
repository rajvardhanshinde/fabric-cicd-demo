name: Deploy to Fabric (DEV / PROD)

on:
  push:
    branches: [ dev, main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      AZURE_CLIENT_ID:     ${{ secrets.AZURE_CLIENT_ID }}
      AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      AZURE_TENANT_ID:     ${{ secrets.AZURE_TENANT_ID }}

  # ---- Steps ----
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Debug context
        run: |
          echo "Branch: ${GITHUB_REF_NAME}"
          echo "Repo:   ${GITHUB_REPOSITORY}"
          echo "Dir:    ${GITHUB_WORKSPACE}"
          ls -R

      # Login with SP (no subscription id)
      - name: Azure Login (Service Principal)
        uses: azure/login@v1
        with:
          client-id:     ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id:     ${{ secrets.AZURE_TENANT_ID }}
          client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r scripts/requirements.txt

      # (Optional but helpful) List workspaces visible to this SP
      - name: Preflight Fabric visibility
        run: |
          pip install --quiet azure-cli jq
          TOKEN=$(az account get-access-token --resource=https://analysis.windows.net/powerbi/api --query accessToken -o tsv)
          echo "Workspaces visible to SP:"
          curl -s -H "Authorization: Bearer $TOKEN" https://api.powerbi.com/v1.0/myorg/groups \
            | jq -r '.value[] | "\(.name) (\(.id))"'

      # Branch → Workspace mapping
      - name: Deploy to Fabric
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REF_NAME}" == "dev" ]]; then
            echo "🚀 DEV branch → DEV workspace: ${{ secrets.FABRIC_DEV_WORKSPACE_ID }}"
            python scripts/deploy_fabric_items.py \
              --WorkspaceId "${{ secrets.FABRIC_DEV_WORKSPACE_ID }}" \
              --Environment DEV \
              --RepositoryDirectory "${GITHUB_WORKSPACE}" \
              --ItemsInScope all \
              --Debug
          elif [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            echo "🚀 MAIN branch → PROD workspace: ${{ secrets.FABRIC_PROD_WORKSPACE_ID }}"
            python scripts/deploy_fabric_items.py \
              --WorkspaceId "${{ secrets.FABRIC_PROD_WORKSPACE_ID }}" \
              --Environment PROD \
              --RepositoryDirectory "${GITHUB_WORKSPACE}" \
              --ItemsInScope all \
              --Debug
          else
            echo "⚠️ Branch ${GITHUB_REF_NAME} not mapped. Skipping."
          fi
