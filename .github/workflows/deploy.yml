name: Deploy to Fabric (DEV / PROD)

on:
  push:
    branches:
      - dev
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # --------------------------
      # Checkout repo
      # --------------------------
      - name: Checkout repository
        uses: actions/checkout@v4

      # --------------------------
      # Debug info
      # --------------------------
      - name: Print debug info
        run: |
          echo "🔎 Branch: ${GITHUB_REF_NAME}"
          echo "🔎 Repo: ${GITHUB_REPOSITORY}"
          echo "🔎 Workspace DEV: ${{ secrets.FABRIC_DEV_WORKSPACE_ID }}"
          echo "🔎 Workspace PROD: ${{ secrets.FABRIC_PROD_WORKSPACE_ID }}"
          echo "🔎 Listing repo contents..."
          ls -R

      # --------------------------
      # Login to Azure
      # --------------------------
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # --------------------------
      # Setup Python
      # --------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r scripts/requirements.txt

      # --------------------------
      # Deploy to Fabric
      # --------------------------
      - name: Deploy Fabric Artifacts
        run: |
          if [[ "${GITHUB_REF_NAME}" == "dev" ]]; then
            echo "🚀 Deploying to DEV workspace..."
            python scripts/deploy_fabric_items.py \
              --WorkspaceId "${{ secrets.FABRIC_DEV_WORKSPACE_ID }}" \
              --Environment DEV \
              --RepositoryDirectory "${GITHUB_WORKSPACE}" \
              --ItemsInScope all \
              --Debug
          elif [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            echo "🚀 Deploying to PROD workspace..."
            python scripts/deploy_fabric_items.py \
              --WorkspaceId "${{ secrets.FABRIC_PROD_WORKSPACE_ID }}" \
              --Environment PROD \
              --RepositoryDirectory "${GITHUB_WORKSPACE}" \
              --ItemsInScope all \
              --Debug
          else
            echo "⚠️ Skipping deployment — branch ${GITHUB_REF_NAME} not mapped."
          fi
